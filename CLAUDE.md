# CLAUDE.md



Behavioral guidelines cut common LLM coding mistakes. Merge with project instructions as needed.



**Tradeoff:** Guidelines bias caution over speed. Trivial tasks: use judgment.

**Use RTK:** Always use rtk for all git commands.



## 1. Think Before Coding



**Don't assume. Don't hide confusion. Surface tradeoffs.**



Before implementing:

- State assumptions explicitly. Uncertain → ask.

- Multiple interpretations exist → present them, don't pick silently.

- Simpler approach exists → say so. Push back when warranted.

- Something unclear → stop. Name confusion. Ask.



## 2. Simplicity First



**Minimum code that solves problem. Nothing speculative.**



- No features beyond what was asked.

- No abstractions for single-use code.

- No unrequested "flexibility" or "configurability".

- No error handling for impossible scenarios.

- 200 lines but could be 50 → rewrite.



Ask: "Would senior engineer call this overcomplicated?" Yes → simplify.



## 3. Surgical Changes



**Touch only what you must. Clean only your own mess.**



Editing existing code:

- Don't "improve" adjacent code, comments, formatting.

- Don't refactor things not broken.

- Match existing style, even if you'd do it differently.

- Notice unrelated dead code → mention it, don't delete.



Your changes create orphans:

- Remove imports/variables/functions YOUR changes made unused.

- Don't remove pre-existing dead code unless asked.



Test: every changed line traces directly to user's request.



## 4. Goal-Driven Execution



**Define success criteria. Loop until verified.**



Transform tasks into verifiable goals:

- "Add validation" → "Write tests for invalid inputs, then make them pass"

- "Fix the bug" → "Write a test that reproduces it, then make it pass"

- "Refactor X" → "Ensure tests pass before and after"



Multi-step tasks, state brief plan:

```

1. [Step] → verify: [check]

2. [Step] → verify: [check]

3. [Step] → verify: [check]

```



Strong criteria → loop independently. Weak criteria ("make it work") → constant clarification.



---



**Guidelines working if:** fewer unnecessary diff changes, fewer rewrites from overcomplication, clarifying questions before implementation not after mistakes.

<!-- headroom:learn:start -->
## Headroom Learned Patterns
*Auto-generated by `headroom learn` on 2026-06-28 — do not edit manually*

### File Structure — Large Files
*~4,000 tokens/session saved*
- `orchestrator/app/workflow.py` grows to 26–35 KB per epic (not readable in one call). Use `limit`+`offset` or `git show <sha>:path | grep -n ...` for targeted reads.
- `orchestrator/tests/test_workflow.py` exceeds 54 KB post-E3.S2. Always paginate; prefer targeted grep/git-show to avoid multi-chunk reads.

### Environment — Python & Test Commands
*~3,500 tokens/session saved*
- Never use `python3` (not found) or bare `python` (resolves to Anaconda env without google-adk). Use `uv run python` or `.venv/Scripts/python.exe` from `orchestrator/`.
- Run tests: `cd orchestrator && uv run pytest` (or `.venv/Scripts/python.exe -m pytest`). Run lint: `uv run ruff check app/ tests/` or `.venv/Scripts/ruff.exe check`.
- Verify ADK version: `cd orchestrator && .venv/Scripts/python.exe -c "import google.adk; print(google.adk.__version__)"`

### Environment — RTK Path
*~2,500 tokens/session saved*
- `rtk` is on Bash PATH by default in subagents.
- RTK works in PowerShell via full path: `C:\Users\samar\.headroom\bin\rtk.exe`. PowerShell `$env:PATH` may also include it.

### Environment — Windows Bash Paths
*~2,000 tokens/session saved*
- In Bash, use POSIX paths: `/c/Users/samar/PycharmProjects/GoogleADK/LocalGPT_Rebase`. Windows-style backslash paths (`C:\Users\...`) in `cd` silently strip backslashes → wrong directory.
- Never use a trailing backslash on directory paths in Bash — causes `unexpected EOF` matching error.
- Always quote paths containing spaces. Mixed-slash paths (forward slash with `C:` prefix) also fail.

### ADK Source Inspection
*~1,500 tokens/session saved*
- ADK 2.3.0 installed at `orchestrator/.venv/Lib/site-packages/google/adk/`. Key files: `workflow/`, `events/request_input.py`, `sessions/database_session_service.py`, `runners.py`, `plugins/base_plugin.py`.
- `google.adk.workflow` subpackage only exists inside the project venv — never the system Anaconda env.

### Tool Usage — Read Offset Type
*~1,000 tokens/session saved*
- The `Read` tool `offset` parameter must be a **number** (integer line number), not a string. Passing a string causes `InputValidationError: The parameter 'offset' type is expected as 'number' but provided as 'string'`. Always pass numeric literals.

### File Structure — claudedocs Gitignored
*~500 tokens/session saved*
- `claudedocs/` is listed in `.gitignore`. `git add claudedocs/` fails with "paths are ignored". Use `git add -f claudedocs/<file>` to force-add execution plans and migration docs.

<!-- headroom:learn:end -->

## Agent skills

### Issue tracker

Issues and PRDs are tracked as GitHub issues via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context: `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
