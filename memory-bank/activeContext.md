# Active Context

## Current Phase — v1→v2 MIGRATION IN PROGRESS (2026-06-28): E0 + E1 (P0) + E2.S1 (P1) + E2.S2 (P2) + E3.S1 (P3) + E3.S2 (P4) + E4 (P5) + E5.S1 (P6) landed
§15 migration executing. Plans: [../claudedocs/workflow_v1_to_v2_migration.md] (phases P0–P7) + [../claudedocs/spawn_v1_to_v2_migration.md] (epics E0–E5). **Done: E1 (P0 Tier-0) + E0 (live-path hardening), E2.S1 (P1 App + one-node Workflow skeleton), E2.S2 (P2 research-loop port), E3.S1 (P3 real RequestInput CP2/CP3 + reject path), E3.S2 (P4 flip session ownership to ADK), E4 (P5 local-first A2A exposure), E5.S1 (P6 retire v1 shell).** Only E5.S2 (P7 live E2E on real HW) remains.
- **E1 / P0 ✅** — pin `google-adk[a2a]>=2.3,<3` (lock=2.3.0, +`a2a-sdk` 0.3.26); `OLLAMA_API_BASE` env set in `build_model()`; `MCPToolset.close()` per drive call in `pipeline.py` (leak fix). *Note:* "toolset once-per-run" infeasible in v1's `asyncio.run`-per-handler bridge → **delivered in E2.S2.T4 (node path).**
- **E0 ✅** — crawl4ai stdout→stderr (fd-level guard `dup2(2,1)` in `mcp/Crawl4AI_MCP/main.py`); agent JSON robustness (`jsonio.invoke_json_with_retry` + degrade-to-empty). E0.S3 (real-HW probe) ⏳ pending user.
- **E2.S1 / P1 ✅ (2026-06-24)** — v2 shell stood up on `clarify → plan → CP1` slice. New `app/workflow.py` (`research` `@node(rerun_on_resume=True)` driving `ctx.run_node(clarifier)`→`ctx.run_node(planner)`→CP1; `build_research_workflow(*, clarifier_node, planner_node, store)` closure DI seam mirroring v1 `PipelineDeps`), `app/adk_app.py` (`app = App(name="localgpt_research", root_agent=Workflow(name="research"), resumability_config=ResumabilityConfig(is_resumable=True))`), `tests/test_workflow.py`. **CP1 already real `RequestInput(response_schema=ResearchPlan)`** (T4 done here, ahead of P3). **T6** `SessionStore` write-through additive (demoted E3.S2). **HARD GATE T5 PASSED** — kill mid-CP1 + resume-by-`invocation_id` re-runs CP1 only; clarify/plan auto-checkpoint-skipped (proven by body-side call counters staying at 1).
  - **Plan correction (verified vs installed ADK 2.3.0 + adk.dev docs):** `RequestInput` is `from google.adk.events` (not `google.adk.workflow`). **Two ADK behaviors:** START-entry param must be named `node_input`; `ctx.run_node` returns `model_dump()` **dict** → coerced back via agents' `parse_clarify_result`/`parse_plan` (latter re-applies depth policy).
- **E2.S2 / P2 ✅ (2026-06-25)** — research loop ported into proven shell (subagent-driven; `cavecrew-reviewer` between tasks + final integration review; ADK anchors re-verified vs 2.3.0 + `adk-docs` MCP).
  - **T1 (`8547088`)** — NEW `app/research_policy.py`: pure loop policy (`ResearchPhase`/`first_phase`/`next_phase`/`depth_budget`/`stop_rule`) **+** relocated pure `merge_ledger` (dedup-by-id, base wins, non-mutating); imports only `config`+`schemas`. `stage_machine.py` re-exports for v1; `pipeline._merge_ledger` delegates → **one** dedup/stop-rule impl shared by v1+v2 (parity meaningful).
  - **T2+T3 (`8f0ace0`)** — loop in `research` node after CP1: `for subtopic in approved.subtopics:` `while not stop_rule(ledger, subtopic, iteration=, new_claims=, budget=depth_budget(approved.depth)):` → `parse_scored_urls(ctx.run_node(acquirer, …))` → [CP3 seam] → `ctx.run_node(extractor, urls)` → `enrich_ledger(parse_ledger(ctx.run_node(verifier, subtopic.question)))` → `merge_ledger`; single accumulating `ClaimLedger`. Deterministic schedule order, **no custom `run_id`**. DI seam extended with `acquirer_node`/`extractor_node`/`verifier_node`. **Parity fix:** node path re-applies `enrich_ledger` (verifier node skips v1's auto-enrich).
  - **T4+T5 (`17d7be4`)** — **T4:** each owned `MCPToolset` built once at loop entry, shared across all passes, `close()`d in `finally`; injected = caller-owned (left open) → delivers E1.T3-deferred lifecycle (`MCPToolset.close()` verified async+idempotent). **T5:** `cp3_hook` DI seam gated on `approved.depth == Depth.DEEP` (mirrors v1 `next_phase` MID_ACQUIRE); shallow/normal skip; no `RequestInput` yet → E3.S1.T2.
  - **T6 (`0dd0bfe`) — HARD GATE PASSED:** `tests/test_parity_v1_v2.py` drives v1 (`Orchestrator._run_research`) vs v2 (`workflow` loop) on identical canned inputs (one source-of-truth JSON set); terminal `ClaimLedger` structurally equal (claims by id + per-subtopic `kept_count`) over ≥1 shallow + 2 deep + all 3 stop-rule exits. Genuinely independent drivers, share only pure policy. + resume-mid-loop test (each scheduled pass runs exactly once across resume boundary).
- **E3.S1 / P3 ✅ (2026-06-25)** — real `RequestInput` CP2/CP3 + reject path (subagent-driven; fresh subagent per task + `cavecrew-reviewer` between; ADK 2.3.0 re-verified via `adk-docs` MCP). Order: T0 ‖ T2 → T1 → T3 → T4 (tracking).
  - **T0 (`21907e0`)** — Writer node wired; DI seam `writer_node=None` on `build_research_workflow`; renders markdown draft via `render_report()` after loop; draft carried to CP2. Terminal output stays `ClaimLedger` (parity-neutral, additive).
  - **T2 (`dd72272`)** — NEW pure `app/cp3_adapter.py`: `apply_cp3_verbs(urls, reply) -> (filtered_urls, needs_supplemental)` ports v1 verbs exactly (`+ <url>` add, `- <n>` exclude, `r <url>` redirect, `d` done); `_user_url -> ScoredURL(score=1.0, strategy=CRAWL, source="user")`; imports config+schemas only. CP3 = real deep-only `RequestInput` node (`_make_cp3_checkpoint(interrupt_id=f"cp3_{subtopic.id}_{iteration}")`); `needs_supplemental` → acquirer re-entry before extract. +11 unit tests `test_cp3_adapter.py`. `cp3_hook` seam removed.
  - **T1 (`57d1755`)** — `_cp2_checkpoint`/`_make_cp2_checkpoint` `@node(rerun_on_resume=False)` yields `RequestInput(payload=draft, response_schema=str)`; unconditional; parity driver answers with empty reply (approve, ledger-neutral).
  - **T3 (`522f0c4`)** — **reject = resumable abort via re-yield `RequestInput`** (ADK 2.3.0: raise = terminal `NodeStatus.FAILED`, non-resumable). Reject signals: CP1 = empty-subtopics plan; CP2/CP3 = reserved `"__reject__"` (`CHECKPOINT_REJECT`). 4 reject tests + no-rerun-of-clarify/plan test.
  - **T4** — tracking only. `app/checkpoint.py` delete deferred → **E5.S1.T6** (still imported by v1 shell `cp1/cp2/cp3_handler`). Nothing deleted.
- **E3.S2 / P4 ✅ (2026-06-26)** — session ownership flipped to ADK; `SessionStore` demoted to exporter; `stage.json` retired as truth for v2 (subagent-driven; `cavecrew-reviewer` between each task; ADK 2.3.0 re-verified via `adk-docs` MCP).
  - **T1 (`2217d73`)** — `DatabaseSessionService` + `build_runner()` factory + `yield Event(state={…})` state-delta calls in `research` @node (`v2_plan`/`v2_ledger`/`v2_draft`/loop markers). Plan A confirmed: `yield Event(state=…)` from inside `@node` generator IS durable. `research` converted from coroutine to async generator.
  - **T2 (`19bf987`)** — NEW `app/session_exporter.py` (`SessionExporterPlugin(BasePlugin)`): `on_event_callback` → incremental writes from `event.actions.state_delta`; `after_run_callback` → final flush. Writes `plan.json`/`claim_ledger.json`/`draft.md` byte-identical to `SessionStore`. No `stage.json`.
  - **T3 (`27e7c6d`)** — `store=` write-through seam removed from `workflow.py` (3 blocks + param + import). Surgical: `SessionStore` class in `session.py` untouched.
  - **T4 (`62e8ba5`)** — Test callers migrated off `store=`; `test_store_write_through_persists_ledger` deleted (seam-only test).
  - **T5 (`e283f35`+`b03b339`)** — **HARD GATE #3 PASSED**: cross-restart DB resume with exporter PASSED; byte-comparable export vs v1 `SessionStore` PASSED.
- **E4 / P5 ✅ (2026-06-27)** — pipeline published as ONE localhost server exposing REST (`/run_sse`) + A2A protocol (card + RemoteA2aAgent), full HITL pause/resume over wire + cross-restart resume-by-`invocation_id`. Additive — in-process path unbroken. Subagent-driven; `cavecrew-reviewer` between each task + final epic review; ADK 2.3.0 re-verified via `adk-docs` MCP. Order: T0 → T1 → T1b (HARD GATE) → {T2 ‖ T3 ‖ T4} → T5.
  - **T0 (`2435b0f`)** — `SessionExporterPlugin` registered on `adk_app.app` (server-built runner bypasses `build_runner` plugins=); `build_runner` dedup-by-`.name` guards double-registration; NEW `localgpt_research/__init__.py` re-export shim so AgentLoader discovers app_name `localgpt_research` under `agents_dir`.
  - **T1 (`adb65a4`)** — NEW `app/server.py`: `get_fast_api_app(agents_dir, session_service_uri=config.SESSION_DB_URL, a2a=True, host="localhost", port=8001, web=False)`; uvicorn `main()` bound localhost-only. **ADK 2.3.0 bug found+worked-around:** `get_fast_api_app(a2a=True)` has function-local `import json` shadowing module → `UnboundLocalError` swallowed → A2A card 404 + no RPC route. Hand-rolled `_mount_a2a`/`mount_a2a_routes` replicates intended wiring (`A2aAgentExecutor`→`DefaultRequestHandler`→`A2AStarletteApplication`, card from static `agent.json`), guarded no-op if route already present (future-ADK-safe).
  - **T1b (`2dd25d3`) — HARD GATE #4 PASSED:** CP1 `RequestInput` pause→resume over A2A protocol via `RemoteA2aAgent(use_legacy=False)` + httpx ASGITransport against DB-backed stub server; node-skip-on-resume proven (clarifier/planner body counters stay `[1]`). Fully offline. Proves ADK 2.3.0 carries RequestInput resume over A2A.
  - **T2 (`ceae7ba`)** — agent card content: `defaultOutputModes=["text/markdown"]` (pipeline returns markdown), `defaultInputModes=["text/plain"]`, new-executor A2A extension advertised under `capabilities.extensions` (URI `https://google.github.io/adk-docs/a2a/a2a-extension/` = `_NEW_A2A_ADK_INTEGRATION_EXTENSION`, verified in installed ADK). +offline card test.
  - **T3 (`88e7872`)** — NEW `app/cli.py`: thin httpx CLI over REST. Create session → `POST /run_sse` → parse SSE → detect `adk_request_input` (via `has_request_input_function_call`) → render CP prompt REUSING `checkpoint.py` + `cp3_adapter.apply_cp3_verbs` → answer via `create_request_input_response` → resume by `invocation_id`. Terminal ledger arrives as `event.output` on SSE, draft via `v2_draft` state_delta. +offline full-run test.
  - **T4 (`9c54235`)** — NEW `app/a2a_client_example.py`: `build_remote_research_agent()` constructs `RemoteA2aAgent(agent_card=f"http://localhost:8001/a2a/localgpt_research{AGENT_CARD_WELL_KNOWN_PATH}", use_legacy=False)`. Doc/example only — specialists NOT split onto own servers. +offline consumption test (reuses T1b harness).
  - **T5 (`ed7227f`) — EPIC ACCEPTANCE GATE PASSED:** `tests/test_a2a_roundtrip.py` — (A) A2A round-trip full HITL (CP1→CP2 normal; CP1→CP3→CP2 deep) over RemoteA2aAgent; (B) REST resume-by-`invocation_id`; (C) parity: wire ledger + byte-exact draft == in-process `_drive_through_checkpoints` result. A2A terminal result read from DB-backed session (ADK `from_adk_event` projects only `content.parts`, not `event.output`).
- **E5.S1 / P6 ✅ (2026-06-28)** — retired dead v1 orchestration shell; v2 ADK dynamic-workflow path owns pipeline. Subagent-driven; `cavecrew-reviewer` after each task (all clean). Refined into [../claudedocs/E5_S1_execution_plan.md] (spec-of-record), corrected written plan in 3 places: `pipeline.py` is 100% v1 driver → **delete whole file** (not "partial machinery"); `stage_machine.py` is pure re-export shim of `research_policy` symbols → **delete whole file** (not "split" — policy already lives in `research_policy.py`); **two live non-test consumers** had to be cut BEFORE any delete. Order: S1 (grep) → S2 (cut consumers) → S3+S4 (deletes + test retirement, landed atomically) → S5 (gate).
  - **S2a (`1e79f20`)** — inlined CP1/CP2 renderers `cli.py` actually calls (`_render_cp1`/`_render_cp2` + `CheckpointRejected` + `_edit_text`/`_launch_editor`, depending only on `schemas`) into `app/cli.py`; dropped `from app import checkpoint` / `from app.checkpoint import CheckpointRejected`. CP3 untouched (cli.py already uses `cp3_adapter.apply_cp3_verbs`). `test_cli.py` green.
  - **S2b (`087018f`)** — rewired `orchestrator/main.py` (v1 console entrypoint) off `pipeline.build_orchestrator`/`run_pipeline` to drive **v2 ADK workflow in-process**: `build_runner(adk_app.app)` → loop answering CP1/CP2/CP3 `RequestInput` pauses via cli.py renderers (`_request_input_call`/`_answer_checkpoint`, imported — single v2 copy) + `create_request_input_response`, resuming by `invocation_id` (mirrors `tests/test_workflow.py::_drive_through_checkpoints` with real console I/O); prints terminal ledger + `data/sessions/{id}/` exporter path. Offline smoke (canned `@node` stubs, monkeypatched `input`) drove CP1→CP2→terminal ledger. No v1-shell import remains.
  - **S3+S4 (`6963cb6`, atomic)** — deleted `app/orchestrator.py`, `app/pipeline.py`, `app/stage_machine.py`, `app/checkpoint.py`; deleted 5 v1-driver test files (`test_orchestrator`/`test_research_loop`/`test_pipeline`/`test_checkpoint`/`test_parity_v1_v2`); migrated 8 pure-policy tests (`first_phase`/`next_phase`/`depth_budget`/`stop_rule`) → NEW `tests/test_research_policy.py` (imports `app.stage_machine`→`app.research_policy`), then deleted `test_stage_machine.py`; stripped `test_toolset_lifecycle.py` to v2 node-path tests (dropped `from app import pipeline` half). E2.S2.T6 parity guard retired (job done; ongoing v2 guard is `test_workflow.py`).
  - **S5 gate ✅** — zero live `orchestrator`/`pipeline`/`stage_machine`/`checkpoint` imports repo-wide; ruff clean; `import app` + `import localgpt_research` (agents-dir discovery → `adk_app.app`) load offline. *Not a v1 delete:* `SessionStore` class (`session.py`) survives — still used by v2 final `write_report`.
- **Suite: 231 orchestrator collected (was 273; net −42 = 5 v1-driver test files + `test_stage_machine` driver tests + `test_toolset_lifecycle` v1 half removed, +8 migrated policy tests) — 230 green offline; 1 (`test_node_path_injected_agent_builds_no_toolset`) needs live Ollama (injects no `writer_node` → real Writer call), confirmed pre-existing (red at clean HEAD too) and green on HW baseline. 43 MCP green, ruff clean. COMMITTED** to `dev` — E5.S1 `1e79f20`/`087018f`/`6963cb6`; E4 `2435b0f`/`adb65a4`/`2dd25d3`/`ceae7ba`/`88e7872`/`9c54235`/`ed7227f`; E3.S2 `2217d73`/`19bf987`/`27e7c6d`/`62e8ba5`/`e283f35`/`b03b339`; E3.S1 `21907e0`/`dd72272`/`57d1755`/`522f0c4`; E2.S2 `8547088`/`8f0ace0`/`17d7be4`/`0dd0bfe`; E2.S1 `21330ef`/`306f8d7`/`95cc51b`/`850b75b`. Not pushed.
- **Next = E5.S2 / P7** (live E2E on real hardware: AC1–AC7 + resume + A2A round-trip + OOM < 16 GB RAM / VRAM < 14 GB, needs E0.S3 model pull). All 4 HARD GATES green (E2.S1 resume, E2.S2 parity, E3.S2 byte-comparable export, E4 A2A HITL round-trip). v1 shell **gone** — migration code-complete; only real-HW acceptance run remains.

**Design baseline (v2, ratified 2026-06-23):** **ADK 2.x**, **dynamic-workflow** orchestration (`@node`/`Workflow`/`ctx.run_node`), **ADK-owned sessions/resume** (`ResumabilityConfig`; `data/sessions/*.json` → write-through export), `RequestInput` HITL, **single local-first A2A boundary** (`to_a2a` / `adk api_server --a2a`, localhost) with 6 specialists kept as local nodes (not A2A peers). Master spec: [architecture.md] (§0/§13/§14/§15). User decisions: ADK 2.x ✓, ADK-owned sessions ✓, A2A local-first ✓, deploy = `adk api_server` ✓.

## Prior Phase (v1 build — complete)
**Story 5 (Integration & Research Loop) — DONE + offline-tested; live path validated to agent-output boundary.** Built 2026-06-23: T4.1 per-subtopic research loop + stop-rule, T4.2 composition root (`app/pipeline.py`) wiring all six real agents + CP1/CP2/CP3, T4.3 adaptive depth→iteration budgets. orchestrator **203** tests green (was 181: +stop-rule/research-loop/pipeline/jsonio), MCP **43** green, ruff clean. Earlier: Story 2/3/4 done.

**Live smoke (user bar = live run):** reached RESEARCH + exercised real chain — Ollama Clarifier→Planner, MCP subprocess launch, `discover_urls` (28 real URLs), triage scoring, fenced-JSON parsing. Did NOT reach final report; blocked by two issues *outside* Story-5 wiring (see Known Issues). Composition-root integration proven correct.

## Current Focus
Pipeline has: shared one-hot model factory (`app/llm.py`, ADK `LiteLlm`→Ollama), deterministic stage machine + orchestrator skeleton (INTAKE→DONE, deep-only CP3 via `ResearchPhase.MID_ACQUIRE`), all six agents (Clarifier/Planner/Acquirer/Extractor/Verifier/Writer), Semantic Scholar citation client (`app/citation.py`), Story 2 MCP extensions (eTLD+1, dedup, seed ingest, PDF routing, rate-limit backoff), Story 4 checkpoints (`app/checkpoint.py`: `cp1/cp2/cp3_checkpoint` + `Handler` wrappers; `post_handlers`/`phase_handlers` hooks in orchestrator; `SessionStore.save/load_scored_urls` for CP3's edited source list). All tests run offline (mocked/stubbed model, monkeypatched stdin/editor). **Story 4 nuance**: checkpoint *mechanism* + handlers landed and G5-verified by tests registering them directly; registration into live composition root happens with Story 5 integration (alongside real agent wiring — skeleton still uses stub handlers).

## Recent Decisions (from brainstorm)
- Verification = cross-corroborate-or-flag (no per-claim confidence scoring required).
- Autonomy = checkpoint approvals (plan + draft gates).
- Depth = adaptive (query-driven).
- Framework = open to recommendation (NOT locked to Google ADK).
- Clarification triggers = ambiguous scope + missing constraints, before run start only.
- Sources = mixed; current MCP aggregates SerpAPI + DuckDuckGo + arXiv, to be improved.
- Output = structured report (exec summary / sections / sources / contradictions appendix).

## Design Decisions (this phase)
- **Framework**: Google ADK + A2A (kept — MCP already ADK-integrated; RAM-neutral).
- **Topology**: hierarchical coordinator — Orchestrator delegates to Clarifier/Planner/Acquirer/Extractor/Verifier/Writer.
- **Model**: one hot Qwen2.5-14B Q4 (8B fallback) role-prompted + resident nomic-embed-text. ~12.5 GB VRAM. Sequential stages (16 GB RAM bottleneck).
- **Tool access**: only Acquirer/Extractor hold MCP write tools (least privilege).
- Open questions Q1–Q6 resolved in [architecture.md] (model fit, independent-source = eTLD+1 + cosine<0.92, MCP 5 extensions, resume via stage.json, stop-rule, CLI checkpoints).

## Story 5 — DONE (2026-06-23)
- **T4.1 Research loop** — `Orchestrator._run_research` (per-subtopic, budget-bounded) + `stage_machine.stop_rule`/`depth_budget`; ledger accumulates across passes; phase handlers ACQUIRE/EXTRACT/VERIFY.
- **T4.2 Composition root** — `app/pipeline.py` `build_orchestrator()`/`run_pipeline()`: wires six agents, CP1→`post_handlers[PLAN]`, CP2→`post_handlers[WRITE]`, CP3→`phase_handlers[MID_ACQUIRE]`; sync↔async bridge (`_run_sync`); `CheckpointRejected` abort + report publish. `main.py` rewired.
- **T4.3 Adaptive depth** — `config.MAX_ITER_{SHALLOW,NORMAL,DEEP}` → `depth_budget(depth)` consumed by stop-rule. target_evidence/CP3/citation-BFS depth-gated.
- **Dep fix**: added `mcp` to `orchestrator/pyproject.toml` (ADK `MCPToolset` requires `mcp` SDK; was undeclared, masked by lazy import + offline fakes).
- **Robustness**: `app/jsonio.py loads_first_json` (tolerates code fences + trailing prose) wired into `acquirer.parse_scored_urls` + `verifier.parse_ledger`.
- **Verifier**: added `_build_default_toolset()` (filter `search_chunks`) for live chunk retrieval.

## Known Issues / Follow-ups (live path — NOT Story-5 wiring)
1. ✅ **RESOLVED (E0.S1, 2026-06-23)** — crawl4ai MCP stdout pollution (`[INIT]... Crawl4AI` banner corrupting stdio JSON-RPC). Fixed by fd-level guard in `mcp/Crawl4AI_MCP/main.py`: `dup2(2,1)` repoints fd-1 to stderr + `sys.stdout` rebound over saved real-stdout fd so MCP keeps clean JSON-RPC channel. MCP 43 green.
2. ✅ **RESOLVED (E0.S2, 2026-06-23)** — agent JSON reliability under tool timeouts (Acquirer/Verifier returning non-JSON prose → `loads_first_json` raises). Fixed by `jsonio.invoke_json_with_retry` (one JSON-only re-ask) + graceful degrade (`acquire→[]`, `verify→empty ledger`); genuine Pydantic `ValidationError` still surfaces.
3. ⏳ **PENDING USER (E0.S3)** — run setup probe on real hardware: `cd orchestrator && python scripts/check_setup.py` — pull `qwen2.5:14b-instruct-q4_K_M` + embed; confirm VRAM < 14 GB (Gate G2).

## Active Considerations
- 16 GB RAM binding constraint, not VRAM → sequential execution structural.
- crawl4ai MCP exists (`mcp/Crawl4AI_MCP/`) — extend (6 additions including Semantic Scholar client), don't rebuild.
- Verifier independence test depends on MCP extensions #1 (dedup) + #2 (eTLD+1) — build those first.
- Semantic Scholar citation BFS + temporal drift detection = zero new models, zero VRAM overhead.
- CP3 fires only on `depth=deep` — shallow/normal plans unaffected, no friction added.
- Knowledge graph + session comparison deferred to Story 7 (post-MVP).

## Design Decisions (2026-06-21 update — competitive research + brainstorm)
- **CP3 scope**: fires only for `depth=deep` plans. User approved.
- **Knowledge graph**: deferred to Story 7. Not in v1 scope.
- **Citation BFS depth**: 2 hops. User approved. Bounded by relevance gate + per-subtopic budget cap.
- **P2/P4 integration**: contradiction confidence scoring + temporal drift detection extend Verifier (T2.6/S3.6). No new agents.
- **T1.6**: Semantic Scholar citation graph API client lives in `orchestrator/app/citation.py` (httpx utility), not inside MCP server. Called directly by Acquirer agent.