"""
Specialist agents (Story 3). Each module defines one role-prompted agent built
on the shared model factory in ``app.llm``. Import agents directly from their
modules (e.g. ``from app.agents.planner import build_planner``); this package
marker intentionally stays empty so parallel agent tracks never edit it.
"""
